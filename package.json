{
  "name": "@merkie/json-heal",
  "version": "0.4.6",
  "description": "Heal a cut off json string to make it parseable again",
  "author": "merkie",
  "license": "MIT",
  "repository": "merkie/json-heal",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc -p tsconfig.json",
    "test": "jest --config jest.config.js",
    "dev": "ts-node src/index.ts",
    "prepare": "npm run build"
  },
  "keywords": [
    "json",
    "heal"
  ],
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^18.19.64",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "ts-node": "^10.9.2"
  }
}
